server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetchRegistry: false
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka

  server:
    waitTimeInMsWhenSyncEmpty: 0

  instance:
    hostname:  localhost